#include "main.h"

/**
 * _strelen - returns the length of a string
 * @n: the string
 * Return: the length of given string
 */

int _strlen(char *s)
{
	int n;

	i = 0;
	while (s[i])
	{
		i++;
	}
	return (i);
}
